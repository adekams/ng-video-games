// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

// IMPORTANT: Copy this file to environment.ts and add your actual API keys
// Do NOT commit environment.ts to git - it's already in .gitignore

export const environment = {
  production: false,
  API_URL: 'https://rawg-video-games-database.p.rapidapi.com',
  RAPIDAPI_KEY: 'YOUR_RAPIDAPI_KEY_HERE', // Get from https://rapidapi.com/api-sports/api/api-sports
  RAWG_API_KEY: 'YOUR_RAWG_API_KEY_HERE', // Get from https://rawg.io/apidocs
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
